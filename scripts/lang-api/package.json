{
	"name": "asb-lang-api",
	"version": "1.0.0",
	"description": "Local franc-based language detection service, so LangSpam can be compared without calling the public API.",
	"type": "module",
	"main": "server.mjs",
	"dependencies": {
		"franc": "6.2.0"
	}
}
